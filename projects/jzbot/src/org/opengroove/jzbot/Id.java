package org.opengroove.jzbot;

/**
 * An identifier. An identifier can identify either a person, a room, or a
 * server. People are always under servers, and so are rooms. In other words,
 * there is never a person located under a room.
 * 
 * multiple nicks per authname, one authname per nick, don't store persistent
 * info based on nick except in rare circumstances, warn about not being auth'd
 * in that case, 
 * 
 * @author Alexander Boyd
 * 
 */
public class Id
{
    
}
